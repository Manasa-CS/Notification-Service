# Notification-Service
This repository hosts a Spring Boot microservice that consumes notification events from a Kafka topic and sends notifications via multiple channels (Email, SMS, Push). This project demonstrates event-driven architecture, polymorphic DTO design, and clean, extensible service patterns.
